USE BDX;
GO
DROP TABLE IF EXISTS dbo.BO_EMAILS_SEND_LOG
GO
CREATE TABLE dbo.BO_EMAILS_SEND_LOG
(
	ID_LOG BIGINT NOT NULL,
	ID_ENTIDAD SMALLINT NOT NULL,
	ENTIDAD_NOMBRE VARCHAR(30) NOT NULL,
	MENSAJE VARCHAR(MAX),
	IDENTIFICADOR VARCHAR(50) NOT NULL,
	CONSTRAINT FK_ID_LOG FOREIGN KEY (ID_LOG)
	REFERENCES BO_LOG(ID)
)
GO
